"use strict";
// const subheader: HTMLElement | null = document.querySelector('.header .sub-header');
// if (subheader != null) {
//    const text = subheader.innerText;
//    subheader.innerText = '';
//    text.split('').forEach(e => {
//       const span = document.createElement('span');
//       span.innerText = e;
//       subheader.appendChild(span);
//    });
// }
